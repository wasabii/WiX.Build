<Project>
    <Import Project="$(MSBuildThisFileDirectory)build\Common.props" />

    <PropertyGroup>
        <RunSettingsFilePath>$(SolutionDir)\WiX.Build.runsettings</RunSettingsFilePath>
        <GenerateDependencyFile>true</GenerateDependencyFile>
    </PropertyGroup>

</Project>
